<div class="p-6">
    @if($successMessage)
    <div x-data="{ show: true }" x-init="setTimeout(() => show = false, 3000)" x-show="show"
        class="mb-6 p-4 bg-green-50 border-l-4 border-green-500 text-green-700">
        <div class="flex items-center">
            <svg class="h-5 w-5 text-green-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
            </svg>
            <p>{{ $successMessage }}</p>
        </div>
    </div>
    @endif

    <div class="space-y-8">
        <!-- Activities Selection -->
        <div>
            <h2 class="text-xl font-semibold text-gray-800 mb-4">Activities & Services</h2>
            <p class="text-gray-600 mb-6">Select the activities and services your business offers to customers.</p>

            <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
                @foreach($availableActivities as $key => $label)
                <div class="relative">
                    <label class="flex items-center p-4 border rounded-lg cursor-pointer transition-all duration-200
                        {{ in_array($key, $activities) ? 'bg-teal-50 border-teal-500' : 'bg-white border-gray-200 hover:bg-gray-50' }}">
                        <input type="checkbox" wire:click="toggleActivity('{{ $key }}')"
                            {{ in_array($key, $activities) ? 'checked' : '' }}
                            class="form-checkbox h-5 w-5 text-teal-600 rounded border-gray-300 focus:ring-teal-500">
                        <span class="ml-3 text-gray-700">{{ $label }}</span>
                    </label>
                </div>
                @endforeach
            </div>
        </div>

        <!-- Shop Rules -->
        <div>
            <h2 class="text-xl font-semibold text-gray-800 mb-4">Shop Rules</h2>
            <p class="text-gray-600 mb-6">Define rules that customers should follow when using your services.</p>

            <div class="space-y-4">
                <div class="flex items-center space-x-2">
                    <input type="text" wire:model="newRule" wire:keydown.enter="addRule"
                        class="flex-1 rounded-md border-gray-300 shadow-sm focus:border-teal-500 focus:ring focus:ring-teal-500 focus:ring-opacity-50"
                        placeholder="Add a new rule...">
                    <button type="button" wire:click="addRule"
                        class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-teal-600 hover:bg-teal-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-teal-500">
                        Add
                    </button>
                </div>

                <ul class="space-y-2">
                    @forelse($rules as $index => $rule)
                    <li class="flex items-center justify-between p-3 bg-white border border-gray-200 rounded-md">
                        <span class="text-gray-700">{{ $rule }}</span>
                        <button type="button" wire:click="removeRule({{ $index }})" class="text-red-500 hover:text-red-700">
                            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                            </svg>
                        </button>
                    </li>
                    @empty
                    <li class="p-3 bg-gray-50 border border-gray-200 rounded-md text-gray-500 text-center">
                        No rules added yet. Add your first rule above.
                    </li>
                    @endforelse
                </ul>
            </div>
        </div>

        <!-- Amenities -->
        <div>
            <h2 class="text-xl font-semibold text-gray-800 mb-4">Amenities & Requirements</h2>
            <p class="text-gray-600 mb-6">List amenities you provide or requirements customers should be aware of.</p>

            <div class="space-y-4">
                <div class="flex items-center space-x-2">
                    <input type="text" wire:model="newAmenity" wire:keydown.enter="addAmenity"
                        class="flex-1 rounded-md border-gray-300 shadow-sm focus:border-teal-500 focus:ring focus:ring-teal-500 focus:ring-opacity-50"
                        placeholder="Add an amenity or requirement...">
                    <button type="button" wire:click="addAmenity"
                        class="inline-flex items-center px-4 py-2 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-teal-600 hover:bg-teal-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-teal-500">
                        Add
                    </button>
                </div>

                <ul class="space-y-2">
                    @forelse($amenities as $index => $amenity)
                    <li class="flex items-center justify-between p-3 bg-white border border-gray-200 rounded-md">
                        <span class="text-gray-700">{{ $amenity }}</span>
                        <button type="button" wire:click="removeAmenity({{ $index }})" class="text-red-500 hover:text-red-700">
                            <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path>
                            </svg>
                        </button>
                    </li>
                    @empty
                    <li class="p-3 bg-gray-50 border border-gray-200 rounded-md text-gray-500 text-center">
                        No amenities added yet. Add your first amenity above.
                    </li>
                    @endforelse
                </ul>
            </div>
        </div>

        <!-- Save Button -->
        <div class="pt-4">
            <button type="button" wire:click="save"
                class="inline-flex items-center px-6 py-3 border border-transparent rounded-md shadow-sm text-base font-medium text-white bg-teal-600 hover:bg-teal-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-teal-500">
                <svg class="h-5 w-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                </svg>
                Save Activities Information
            </button>
        </div>
    </div>
</div>
