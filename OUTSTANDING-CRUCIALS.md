## Outstanding / Crucial Items

- Guest landing + navbar locale toggle still not flipping EN↔BM at runtime. `SetLocale` + `/language/{locale}` exist and labels are wrapped with `__()`, but front-end still renders default language; needs deeper investigation and fix.  
